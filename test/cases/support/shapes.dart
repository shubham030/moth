// Imported by imports.dart — lives in a subdirectory so the golden runner
// does not treat it as a case of its own.
final libraryName = 'shapes';

class Shape {
  String name = 'shape';
  String describe() => 'a $name';
}

int twice(int n) => n * 2;
