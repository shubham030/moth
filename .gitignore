# Dart
.dart_tool/
pubspec.lock
build/

# C / ESP-IDF
build-fps/
sdkconfig
sdkconfig.old
managed_components/
dependencies.lock

# moth artifacts
*.mothb
# program.mothb is embedded in the firmware image; checked in so the ESP
# build works without the Dart toolchain.
!vm/esp/main/program.mothb

# OS
.DS_Store

# Docusaurus
website/node_modules/
website/build/
website/.docusaurus/
.cache/

# never ship
.private/
